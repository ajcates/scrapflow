{
  "name": "scrapflow",
  "version": "0.0.1",
  "private": true,
  "workspaces": [
    "client",
    "server"
  ],
  "scripts": {
    "test": "npm test --workspaces",
    "dev": "npm run dev --workspaces"
  },
  "devDependencies": {
    "playwright-core": "^1.56.1"
  }
}
